# Vercel (Frontend) - Steward
# Copy to .env.local for local dev. Set in Vercel Dashboard for Preview/Production.

# Backend (Railway) - use your Railway service URL
VITE_API_BASE_PATH=https://<railway-prod>/api
VITE_WS_BASE_URL=wss://<railway-prod>/ws

# MCP (optional) - same as /api or /api/mcp when used
VITE_MCP_API_BASE_PATH=

# Supabase (anon only; never service role)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
