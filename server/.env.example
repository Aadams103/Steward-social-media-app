# Railway (Backend) - Steward
# Set in Railway Dashboard → Variables. Do NOT commit .env.

NODE_ENV=production
# PORT - Railway sets automatically

# Google OAuth (if used)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_BASE=https://<railway-service-url>

# Meta OAuth (if used)
META_APP_ID=
META_APP_SECRET=
META_OAUTH_REDIRECT_BASE=https://<railway-service-url>

# Supabase (when wired) - backend only; never in frontend
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

# Cron / ingest (optional) - guard /api/cron/ingest
INGEST_SECRET=
CRON_SECRET=
