# Railway configuration note
#
# This repository contains multiple apps.
# For the backend service:
# - Set Railway "Root Directory" to `server`
# - Use `server/railway.toml`
# - Do NOT set custom Build or Start commands in the dashboard
# - Node version should be 22+
#
# This file intentionally contains no build or deploy config.
